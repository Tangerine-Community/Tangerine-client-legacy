class GPSEditView extends Backbone.View

  className : "GPSEditView"

  initialize: ( options ) ->
    @model = options.model
    @parent = options.parent

  render: -> # do nothing

  save: -> # do nothing
  
  isValid: -> true